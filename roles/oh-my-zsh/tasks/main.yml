---
- name: Cloning oh-my-zsh repo
  git:
    repo=https://github.com/robbyrussell/oh-my-zsh
    dest=~/.oh-my-zsh
    umask=0033 todo: uncomment on ansible 2.2
  register: cloning

- name: Set zsh as default shell
  user: name="{{ ansible_user_id }}" shell=/bin/zsh
  become: True
